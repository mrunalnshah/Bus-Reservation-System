#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <string.h>

#define BUFFER_SIZE 20

int login(char username[], char password[], char type);
char* inputUsername();
char* inputPassword();
int registerUser();